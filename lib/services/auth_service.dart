class AuthService {
  static const String username = "budget";
  static const String password = "pebbles";

  bool login(String username, String password) {
    return username == username && password == password;
  }
}
